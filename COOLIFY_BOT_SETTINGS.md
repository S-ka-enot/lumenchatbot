# ⚠️ Исправление настроек Bot сервиса в Coolify

## Проблема на скриншоте

В разделе **Build** → **Dockerfile Location** указано: `/bot/Dockerfile`

**Это неправильно!** Начальный слеш `/` делает путь абсолютным, а Docker ожидает относительный путь от Build Context.

## ✅ Правильные настройки для Bot сервиса

### В разделе "General" → "Build":

1. **Base Directory:** `/` или `.` (корень репозитория) ✅
2. **Dockerfile Location:** `bot/Dockerfile` (БЕЗ начального слеша!) ❌→✅
3. **Docker Build Stage Target:** оставьте пустым
4. **Watch Paths:** можно оставить `src/pages/**` или очистить
5. **Custom Docker Options:** можно очистить (не требуется для бота)

### В разделе "Network":

1. **Ports Exposes:** можно оставить `3000` или убрать (боту порт не нужен)
2. **Ports Mappings:** можно оставить `3000:3000` или убрать

### ⚠️ Важно: Build Context

Если в разделе "General" или "Advanced" есть поле **"Build Context"** или **"Context"**:
- Установите: `.` (точка) или оставьте **пустым**
- Это критически важно!

## 🔧 Как исправить

1. В поле **"Dockerfile Location"** удалите начальный слеш
2. Измените: `/bot/Dockerfile` → `bot/Dockerfile`
3. Нажмите **"Save"**
4. Пересоберите сервис

## ✅ После исправления

В логах сборки должно быть:
- ✅ Успешное копирование `backend/pyproject.toml`
- ✅ Успешная установка зависимостей
- ✅ Успешная сборка образа

## 📋 Напоминание: правильные пути

- **Backend:** Dockerfile Location = `backend/Dockerfile` (без `/`)
- **Bot:** Dockerfile Location = `bot/Dockerfile` (без `/`)
- **Frontend:** Dockerfile Location = `Dockerfile` (если Base Directory = `frontend/`)

Все пути должны быть **относительными** от Build Context, без начального слеша!

