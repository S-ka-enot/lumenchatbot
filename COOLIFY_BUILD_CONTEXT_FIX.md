# ⚠️ КРИТИЧЕСКИ ВАЖНО: Настройка Build Context в Coolify

## Проблема

Если вы видите ошибку:
```
ERROR: "/backend": not found
ERROR: "/bot": not found
```

Это означает, что **Build Context** в Coolify настроен неправильно!

## ✅ Правильная настройка

### Backend сервис

1. Откройте сервис **Backend** в Coolify
2. Перейдите в раздел **Build Settings** или **Dockerfile Settings**
3. Найдите поле **Build Context** или **Context**
4. Установите значение: **`.`** (точка) или оставьте **пустым**
5. В поле **Dockerfile Path** укажите: `backend/Dockerfile`
6. **Сохраните** и пересоберите

### Bot сервис

1. Откройте сервис **Bot** в Coolify
2. Перейдите в раздел **Build Settings**
3. В поле **Build Context** установите: **`.`** (точка) или оставьте **пустым**
4. В поле **Dockerfile Path** укажите: `bot/Dockerfile`
5. **Сохраните** и пересоберите

### Frontend сервис

1. Откройте сервис **Frontend** в Coolify
2. Перейдите в раздел **Build Settings**
3. В поле **Build Context** установите: **`frontend/`**
4. В поле **Dockerfile Path** укажите: **`Dockerfile`**
5. **Сохраните** и пересоберите

## 🔍 Как проверить

После настройки в логах сборки вы должны увидеть:
- ✅ Успешное копирование файлов
- ✅ Установку зависимостей
- ✅ Сборку образа

Если ошибка сохраняется:
1. Убедитесь, что изменения запушены на GitHub: `git push origin main`
2. Проверьте, что в Coolify указана правильная ветка (обычно `main`)
3. Убедитесь, что Build Context установлен правильно (см. выше)

## 📸 Где найти Build Context в Coolify

В зависимости от версии Coolify, поле может называться:
- **Build Context**
- **Context**
- **Docker Build Context**
- **Working Directory**

Обычно находится в разделе:
- **Settings** → **Build & Deploy**
- или **Dockerfile Settings**
- или **Build Configuration**

## ⚡ Быстрое решение

Если не можете найти поле Build Context:
1. Удалите сервис
2. Создайте заново
3. При создании в разделе **Advanced** или **Build Settings** найдите **Build Context**
4. Установите правильное значение сразу

