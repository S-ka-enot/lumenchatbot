# ✅ Настройки Bot по аналогии с Backend

## 🔍 Настройки Backend (работает)

На скриншоте видно, что Backend использует:
- **Base Directory:** `/`
- **Dockerfile Location:** `/Dockerfile`

## ✅ Настройки для Bot (по аналогии)

Если Backend работает с такими настройками, то для Bot нужно:

### В разделе "General" → "Build":

1. **Base Directory:**
   - Установите: `/` (как в Backend)

2. **Dockerfile Location:**
   - Установите: `/bot/Dockerfile` (аналогично `/Dockerfile` для Backend)
   - Это абсолютный путь от корня репозитория

3. **Docker Build Stage Target:**
   - Оставьте **пустым** (как в Backend)

## 📋 Полные настройки Bot

### General → Build:
- **Base Directory:** `/`
- **Dockerfile Location:** `/bot/Dockerfile`
- **Docker Build Stage Target:** пусто

### Network:
- **Ports Exposes:** можно убрать (боту порт не нужен)
- **Ports Mappings:** можно убрать

## ⚠️ Важно

Если Backend работает с Base Directory = `/` и Dockerfile Location = `/Dockerfile`, значит Coolify интерпретирует `/` как корень репозитория (не файловой системы).

Для Bot нужно использовать тот же подход:
- Base Directory = `/` (корень репозитория)
- Dockerfile Location = `/bot/Dockerfile` (путь к Dockerfile от корня репозитория)

## 🔄 Что сделать

1. Откройте Bot сервис
2. Перейдите в "General" → "Build"
3. Установите:
   - **Base Directory:** `/`
   - **Dockerfile Location:** `/bot/Dockerfile`
   - **Docker Build Stage Target:** очистите (оставьте пустым)
4. Нажмите "Save"
5. Нажмите "Deploy"

После этого Bot должен работать так же, как Backend!

