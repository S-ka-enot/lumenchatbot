version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lumenpay-backend
    ports:
      - "8001:8000"
    environment:
      - ENVIRONMENT=production
