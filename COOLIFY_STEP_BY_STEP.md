# Пошаговая инструкция создания приложений в Coolify

## Шаг 1: Создание Backend сервиса

### На начальном экране (который вы видите):

1. **Repository:** `lumenchatbot` ✅ (уже указано)
2. **Branch:** `main` ✅ (уже указано)
3. **Base Directory:** оставьте `/` или укажите `.`
4. **Port:** измените на `8000` (не 3000!)
5. **Is it a static site?:** НЕ отмечайте (оставьте пустым)
6. **Build Pack:** выберите **"Dockerfile"** (не Nixpacks!)

7. Нажмите **"Continue"**

### На следующем экране (Dockerfile Settings):

1. Найдите поле **"Dockerfile Path"** или **"Dockerfile Location"**
2. Укажите: `backend/Dockerfile`
3. Найдите поле **"Build Context"** или **"Context"**
4. Укажите: `.` (точка) или оставьте **пустым**
5. Сохраните и продолжите

## Шаг 2: Создание Bot сервиса

Повторите те же шаги:
1. Repository: `lumenchatbot`
2. Branch: `main`
3. Base Directory: `/` или `.`
4. Port: не требуется (или любой свободный)
5. Is it a static site?: НЕ отмечайте
6. Build Pack: **"Dockerfile"**
7. Continue
8. Dockerfile Path: `bot/Dockerfile`
9. Build Context: `.` (точка) или пусто

## Шаг 3: Создание Frontend сервиса

1. Repository: `lumenchatbot`
2. Branch: `main`
3. Base Directory: `frontend/` (важно!)
4. Port: `80` или `3000`
5. Is it a static site?: **ОТМЕТЬТЕ** ✅ (это статический сайт)
6. Build Pack: **"Dockerfile"**
7. Continue
8. Dockerfile Path: `Dockerfile` (или `frontend/Dockerfile` если Base Directory = `/`)
9. Build Context: `frontend/` (если Base Directory = `/`)

## ⚠️ Важно

- **НЕ используйте Nixpacks** - используйте **Dockerfile**
- **Base Directory** для Backend и Bot должен быть `/` или `.`
- **Base Directory** для Frontend может быть `frontend/` или `/` (зависит от того, как настроен Dockerfile)
- **Port** для Backend: `8000`
- **Port** для Frontend: `80` или `3000`

## 🔄 Если уже создали с неправильными настройками

1. Откройте созданное приложение
2. Перейдите в **Settings** → **Build & Deploy**
3. Найдите **"Dockerfile Settings"** или **"Build Configuration"**
4. Измените:
   - Build Pack на **"Dockerfile"**
   - Dockerfile Path на правильный
   - Build Context на `.` (для Backend/Bot) или `frontend/` (для Frontend)
5. Сохраните и пересоберите

