# 🔧 Исправление Base Directory в Coolify

## ❌ Проблема на скриншоте

- **Base Directory:** `/` (неправильно - это корень файловой системы!)
- **Dockerfile Location:** `/bot/Dockerfile` (неправильно - абсолютный путь)
- **Ошибка:** "The Base Directory field is required."

## ✅ Решение

### Вариант 1: Исправить Base Directory

1. В поле **"Base Directory"** удалите `/`
2. Введите: `.` (одна точка, БЕЗ слеша)
3. Если Coolify не позволяет оставить пустым, введите: `.` (точка)

### Вариант 2: Если Base Directory должен быть `/`

Если Coolify требует, чтобы Base Directory был `/`, то:

1. **Base Directory:** оставьте `/`
2. **Dockerfile Location:** измените на `bot/Dockerfile` (БЕЗ начального `/`)
3. Coolify объединит: `/` + `bot/Dockerfile` = `/bot/Dockerfile` (но это все еще неправильно)

### Вариант 3: Изменить Dockerfile для работы с Build Context = bot/

Если Coolify автоматически устанавливает Build Context в `bot/`, нужно изменить Dockerfile:

1. Скопировать `backend/pyproject.toml` и `backend/poetry.lock` в `bot/` перед сборкой
2. Или изменить Dockerfile, чтобы он использовал относительные пути от `bot/`

Но это не рекомендуется, так как требует изменения структуры проекта.

## 🎯 Рекомендуемое решение

Попробуйте:

1. **Base Directory:** `.` (точка, БЕЗ `/`)
2. **Dockerfile Location:** `bot/Dockerfile` (БЕЗ начального `/`)
3. Сохраните
4. Если появляется ошибка "The Base Directory field is required", попробуйте оставить поле пустым (если Coolify позволяет)

## ⚠️ Важно

`/` в Base Directory означает корень файловой системы контейнера, а не корень репозитория!

Нужно:
- ❌ `/` → ✅ `.` (корень репозитория)
- ❌ `/bot/Dockerfile` → ✅ `bot/Dockerfile` (относительный путь)

