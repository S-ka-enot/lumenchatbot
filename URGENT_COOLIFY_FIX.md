# 🚨 СРОЧНО: Исправление ошибки Build Context в Coolify

## Проблема

Ошибка: `ERROR: "/backend": not found` или `ERROR: "/bot": not found`

**Причина:** Build Context в Coolify настроен неправильно!

## ✅ Решение (пошагово)

### Шаг 1: Загрузите изменения на GitHub

```bash
git push origin main
```

### Шаг 2: Настройте Build Context в Coolify

#### Для сервиса Backend:

1. Откройте Coolify
2. Найдите ваш **Backend** сервис
3. Нажмите на него, чтобы открыть настройки
4. Перейдите в раздел **"Build & Deploy"** или **"Settings"**
5. Найдите секцию **"Dockerfile"** или **"Build Configuration"**
6. Найдите поле с названием:
   - **"Build Context"** 
   - или **"Context"**
   - или **"Docker Build Context"**
   - или **"Working Directory"**
7. **УДАЛИТЕ** все, что там написано (если есть `backend/` или что-то другое)
8. Введите: **`.`** (одна точка) или оставьте **ПУСТЫМ**
9. В поле **"Dockerfile Path"** должно быть: `backend/Dockerfile`
10. **Сохраните** изменения
11. Нажмите **"Redeploy"** или **"Rebuild"**

#### Для сервиса Bot:

1. Откройте ваш **Bot** сервис
2. Сделайте то же самое:
   - **Build Context:** `.` (точка) или пусто
   - **Dockerfile Path:** `bot/Dockerfile`
3. **Сохраните** и **пересоберите**

#### Для сервиса Frontend:

1. Откройте ваш **Frontend** сервис
2. Установите:
   - **Build Context:** `frontend/`
   - **Dockerfile Path:** `Dockerfile`
3. **Сохраните** и **пересоберите**

## 🔍 Где найти Build Context в Coolify?

В зависимости от версии интерфейса:

### Вариант 1 (новый интерфейс):
1. Сервис → **Settings** → **Build & Deploy**
2. Прокрутите до секции **"Dockerfile"**
3. Там будет поле **"Build Context"**

### Вариант 2 (старый интерфейс):
1. Сервис → **Configuration** → **Dockerfile Settings**
2. Найдите **"Build Context"** или **"Context"**

### Вариант 3 (если не видите):
1. Попробуйте **"Advanced Settings"**
2. Или **"Build Configuration"**
3. Или создайте сервис заново - при создании это поле точно есть

## ⚡ Быстрое решение

Если не можете найти поле:

1. **Удалите** сервис Backend (или Bot)
2. **Создайте заново**
3. При создании в разделе **"Advanced"** или **"Build Settings"** сразу установите:
   - **Build Context:** `.` (точка)
   - **Dockerfile Path:** `backend/Dockerfile` (или `bot/Dockerfile` для бота)
4. Сохраните

## ✅ Проверка

После настройки в логах сборки должно быть:
- ✅ `#8 [base 5/7] COPY backend ./backend` - успешно
- ✅ `#10 [base 6/7] COPY bot ./bot` - успешно
- ✅ Установка зависимостей
- ✅ Успешная сборка

## 📞 Если не помогло

1. Убедитесь, что изменения запушены: `git push origin main`
2. В Coolify обновите репозиторий (если есть кнопка "Refresh" или "Pull")
3. Проверьте, что указана правильная ветка (`main`)
4. Попробуйте пересоздать сервис с нуля

