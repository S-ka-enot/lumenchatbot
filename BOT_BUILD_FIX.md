# 🔧 Исправление ошибки сборки Bot сервиса

## Проблема

Ошибка: `ERROR: "/backend/poetry.lock": not found`

**Причина:** Build Context для Bot сервиса настроен неправильно!

## ✅ Решение

### В Coolify для Bot сервиса:

1. Откройте ваш **Bot** сервис в Coolify
2. Перейдите в **Settings** → **Build & Deploy**
3. Найдите секцию **"Dockerfile"** или **"Build Configuration"**
4. Найдите поле **"Build Context"** (может называться "Context")
5. **УДАЛИТЕ** все, что там написано
6. Введите: **`.`** (одна точка) или оставьте **ПУСТЫМ**
7. В поле **"Dockerfile Path"** должно быть: `bot/Dockerfile`
8. **Сохраните** изменения
9. Нажмите **"Redeploy"** или **"Rebuild"**

## 🔍 Почему это важно?

Dockerfile бота копирует файлы из `backend/`:
```dockerfile
COPY backend/pyproject.toml backend/poetry.lock ./
```

Если Build Context = `bot/`, то Docker не видит директорию `backend/`, которая находится на уровень выше.

Если Build Context = `.` (корень), то Docker видит и `backend/`, и `bot/`, и может скопировать нужные файлы.

## ✅ Проверка

После настройки в логах сборки должно быть:
- ✅ `#8 [base 4/5] COPY backend/pyproject.toml backend/poetry.lock ./` - успешно
- ✅ `#10 [base 6/5] COPY . .` - успешно
- ✅ Установка зависимостей через Poetry
- ✅ Успешная сборка

## 📋 Напоминание: правильные настройки для всех сервисов

### Backend:
- Build Context: `.` (точка) или пусто
- Dockerfile Path: `backend/Dockerfile`
- Port: `8000`

### Bot:
- Build Context: `.` (точка) или пусто
- Dockerfile Path: `bot/Dockerfile`
- Port: не требуется

### Frontend:
- Build Context: `frontend/`
- Dockerfile Path: `Dockerfile`
- Port: `80`

