# Настройка Build Context в Coolify

## ⚠️ Важно: Build Context

В Coolify для каждого сервиса нужно правильно настроить **Build Context**. Это критически важно для успешной сборки!

## 📋 Настройки для каждого сервиса

### Backend

- **Build Context:** `.` (корень репозитория) или оставить **пустым**
- **Dockerfile Path:** `backend/Dockerfile`
- **Порт:** `8000`

**Почему:** Dockerfile backend копирует `backend/` и `bot/`, поэтому нужен доступ к корню репозитория.

### Bot

- **Build Context:** `.` (корень репозитория) или оставить **пустым**
- **Dockerfile Path:** `bot/Dockerfile`
- **Порт:** не требуется

**Почему:** Dockerfile bot копирует `backend/pyproject.toml` и весь код, поэтому нужен доступ к корню репозитория.

### Frontend

- **Build Context:** `frontend/`
- **Dockerfile Path:** `Dockerfile` (или `frontend/Dockerfile` если build context = корень)
- **Порт:** `80`

**Почему:** Dockerfile frontend работает внутри директории `frontend/`, копируя `package.json` и исходный код.

## 🔧 Как настроить в Coolify

1. При создании сервиса выберите **Dockerfile**
2. В поле **Build Context** укажите:
   - Для Backend и Bot: `.` или оставьте пустым
   - Для Frontend: `frontend/`
3. В поле **Dockerfile Path** укажите:
   - Backend: `backend/Dockerfile`
   - Bot: `bot/Dockerfile`
   - Frontend: `Dockerfile` (если build context = `frontend/`)

## ❌ Частые ошибки

- **Ошибка:** `"/backend": not found` или `"/bot": not found`
  - **Причина:** Build Context установлен как `backend/` вместо корня
  - **Решение:** Измените Build Context на `.` (корень репозитория)

- **Ошибка:** `"package.json": not found` в frontend
  - **Причина:** Build Context установлен как корень вместо `frontend/`
  - **Решение:** Измените Build Context на `frontend/`

## ✅ Проверка

После настройки проверьте логи сборки:
- Backend/Bot должны видеть директории `backend/` и `bot/`
- Frontend должен видеть `package.json` в корне build context

