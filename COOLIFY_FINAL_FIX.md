# ✅ Финальное решение: Исправление Build Context для Bot

## 🔍 Проблема

В разделе "Advanced" нет поля "Build Context". В вашей версии Coolify Build Context вычисляется из **Base Directory**.

## ✅ Решение: Исправьте Base Directory

Вернитесь в раздел **"General"** → **"Build"** и исправьте:

### 1. Base Directory

**Сейчас:** `./` ❌  
**Должно быть:** `.` (одна точка, БЕЗ слеша в конце) ✅

### 2. Dockerfile Location

**Сейчас:** `/bot/Dockerfile` ❌  
**Должно быть:** `bot/Dockerfile` (БЕЗ начального слеша) ✅

## 📋 Пошаговая инструкция

1. Откройте раздел **"General"** → **"Build"**
2. Найдите поле **"Base Directory"**
3. Измените: `./` → `.` (удалите слеш в конце)
4. Найдите поле **"Dockerfile Location"**
5. Измените: `/bot/Dockerfile` → `bot/Dockerfile` (удалите начальный слеш)
6. Нажмите **"Save"**
7. Нажмите **"Deploy"** (желтая кнопка вверху справа)

## ✅ Проверка

После исправления в логах сборки должно быть:

```
#8 [base 4/5] COPY backend/pyproject.toml backend/poetry.lock ./
#8 DONE (успешно!)
```

Вместо:
```
#8 ERROR: "/backend/poetry.lock": not found
```

## 🔍 Почему это работает

В Coolify:
- **Base Directory** = Build Context (откуда Docker начинает сборку)
- Если Base Directory = `.` → Docker видит корень репозитория (`backend/`, `bot/`, `frontend/`)
- Если Base Directory = `./` → Coolify может интерпретировать это неправильно

**Dockerfile Location** должен быть относительным путем от Base Directory:
- Если Base Directory = `.` → Dockerfile Location = `bot/Dockerfile` ✅
- Если Base Directory = `.` → Dockerfile Location = `/bot/Dockerfile` ❌ (абсолютный путь не работает)

## 📋 Итоговые настройки

### General → Build:
- **Base Directory:** `.` (точка, без `./`)
- **Dockerfile Location:** `bot/Dockerfile` (без `/` в начале)

### Git Source:
- **Repository:** `S-ka-enot/lumenchatbot` ✅
- **Branch:** `main` ✅

### Advanced:
- Все настройки по умолчанию ✅

После этих изменений сборка должна пройти успешно!

