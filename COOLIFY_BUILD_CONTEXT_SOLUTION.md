# ✅ Решение: Build Context для Bot сервиса

## 🔍 Что видно на скриншоте

В разделе **"Git Source"** нет поля "Build Context". Это нормально - в Coolify Build Context может настраиваться по-другому.

## ✅ Решение: Проверьте раздел "Advanced"

1. В левом меню найдите **"Advanced"** (выделено в списке)
2. Откройте раздел **"Advanced"**
3. Там должно быть поле **"Build Context"** или **"Context"**
4. Установите: `.` (точка) или оставьте **пустым**

## 🔄 Альтернативное решение: Base Directory

Если в Coolify Build Context вычисляется из Base Directory:

1. Вернитесь в раздел **"General"** → **"Build"**
2. Убедитесь, что **Base Directory** = `.` (одна точка, БЕЗ `./`)
3. **Dockerfile Location** = `bot/Dockerfile` (БЕЗ начального `/`)

## 📋 Полная проверка настроек

### В разделе "General" → "Build":
- ✅ **Base Directory:** `.` (точка, без `./`)
- ✅ **Dockerfile Location:** `bot/Dockerfile` (без `/` в начале)

### В разделе "Advanced" (если есть):
- ✅ **Build Context:** `.` (точка) или пусто

### В разделе "Git Source":
- ✅ **Repository:** `S-ka-enot/lumenchatbot` (уже правильно)
- ✅ **Branch:** `main` (уже правильно)

## 🎯 Что сделать сейчас

1. **Откройте раздел "Advanced"** (в левом меню)
2. Найдите поле **"Build Context"** или **"Context"**
3. Установите: `.` (точка)
4. Сохраните
5. Пересоберите сервис

## 🔍 Если в "Advanced" тоже нет Build Context

Если в разделе "Advanced" нет поля "Build Context", значит в вашей версии Coolify он вычисляется автоматически из **Base Directory**.

В таком случае:
1. Убедитесь, что **Base Directory** = `.` (одна точка, не `./`)
2. **Dockerfile Location** = `bot/Dockerfile` (без `/`)
3. Сохраните и пересоберите

## ⚠️ Важно

Если Base Directory = `./` (с слешем), Coolify может интерпретировать это как поддиректорию, а не корень проекта.

Измените:
- ❌ `./` → ✅ `.` (одна точка, без слеша)

