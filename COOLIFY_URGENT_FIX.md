# 🚨 СРОЧНО: Исправление Build Context для Bot

## ❌ Текущая ошибка

```
ERROR: "/backend/poetry.lock": not found
```

Coolify все еще не может найти `backend/` директорию, даже после изменения Base Directory.

## ✅ Решение: Проверьте настройки еще раз

### Шаг 1: Откройте раздел "General" → "Build"

1. Убедитесь, что **Base Directory** = `.` (одна точка, БЕЗ `./`)
2. Убедитесь, что **Dockerfile Location** = `bot/Dockerfile` (БЕЗ `/` в начале)

### Шаг 2: Проверьте, сохранились ли изменения

1. Нажмите **"Save"** в разделе "General" → "Build"
2. Убедитесь, что изменения сохранились (перезагрузите страницу и проверьте)

### Шаг 3: Попробуйте пересоздать сервис

Если настройки не помогают:

1. **Удалите** текущий Bot сервис
2. **Создайте новый** Bot сервис
3. При создании установите:
   - **Repository:** `S-ka-enot/lumenchatbot`
   - **Branch:** `main`
   - **Base Directory:** `.` (одна точка)
   - **Build Pack:** `Dockerfile`
   - **Dockerfile Location:** `bot/Dockerfile` (без `/`)
4. Сохраните и запустите деплой

## 🔍 Альтернатива: Проверьте версию Coolify

В разных версиях Coolify Build Context может настраиваться по-разному:

1. Проверьте версию Coolify (обычно внизу страницы или в настройках)
2. Если это старая версия, возможно, нужно использовать другой формат

## 📋 Итоговые настройки для Bot сервиса

### General → Build:
- **Base Directory:** `.` (точка, не `./`)
- **Dockerfile Location:** `bot/Dockerfile` (без `/`)

### Git Source:
- **Repository:** `S-ka-enot/lumenchatbot`
- **Branch:** `main`

### Network:
- **Ports Exposes:** можно убрать (боту порт не нужен)

## ⚠️ Важно

Dockerfile был обновлен и теперь ожидает, что Build Context = корень проекта (`.`).

Если Build Context все еще неправильный, Dockerfile не сможет найти `backend/pyproject.toml` и `backend/poetry.lock`.

## 🆘 Если ничего не помогает

Если после всех попыток ошибка сохраняется:

1. Сделайте скриншот раздела "General" → "Build" с текущими настройками
2. Проверьте, есть ли в Coolify отдельное поле "Build Context" где-то еще
3. Или попробуйте связаться с поддержкой Coolify

