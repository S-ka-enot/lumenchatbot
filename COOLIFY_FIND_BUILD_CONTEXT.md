# 🔍 Где найти Build Context в Coolify для Bot сервиса

## ❌ Текущая ошибка

```
ERROR: "/backend/poetry.lock": not found
```

Это означает, что Build Context установлен неправильно.

## 📍 Где искать Build Context

Build Context в Coolify может быть в разных местах в зависимости от версии:

### Вариант 1: В разделе "Git Source"

1. Откройте ваш **Bot** сервис
2. В левом меню найдите **"Git Source"**
3. Откройте его
4. Найдите поле **"Build Context"** или **"Context"** или **"Build Directory"**
5. Установите: `.` (точка) или оставьте **пустым**

### Вариант 2: В разделе "Advanced"

1. Откройте ваш **Bot** сервис
2. В левом меню найдите **"Advanced"**
3. Откройте его
4. Найдите поле **"Build Context"** или **"Context"**
5. Установите: `.` (точка) или оставьте **пустым**

### Вариант 3: В разделе "General" → "Build"

1. Откройте ваш **Bot** сервис
2. В разделе **"General"** → **"Build"**
3. Найдите поле **"Build Context"** (может быть рядом с "Base Directory")
4. Установите: `.` (точка) или оставьте **пустым**

### Вариант 4: При создании/редактировании сервиса

1. Откройте настройки сервиса
2. Найдите секцию **"Dockerfile Settings"** или **"Build Configuration"**
3. Там должно быть поле **"Build Context"** или **"Context"**

## ✅ Правильные настройки для Bot

### Build Context (где бы он ни был):
- Значение: `.` (одна точка) или **пусто**
- Это означает: корень репозитория (где находятся `backend/`, `bot/`, `frontend/`)

### Base Directory (в General → Build):
- Значение: `.` (одна точка, БЕЗ `./`) или **пусто**

### Dockerfile Location (в General → Build):
- Значение: `bot/Dockerfile` (БЕЗ начального `/`)

## 🔍 Как проверить, что Build Context правильный

После установки Build Context = `.`, в логах сборки должно быть:

```
#7 [internal] load build context
#7 transferring context: ... done
#8 [base 4/5] COPY backend/pyproject.toml backend/poetry.lock ./
#8 DONE (успешно, не ERROR!)
```

Если видите `ERROR: "/backend/poetry.lock": not found` - Build Context все еще неправильный.

## 🆘 Если не можете найти Build Context

Если в Coolify нет явного поля "Build Context":

1. **Попробуйте пересоздать сервис:**
   - Удалите текущий Bot сервис
   - Создайте новый
   - При создании найдите поле "Build Context" или "Context"
   - Установите `.` (точка)

2. **Или проверьте документацию Coolify:**
   - В новых версиях Build Context может называться по-другому
   - Может быть в разделе "Build Settings" или "Deploy Settings"

## 📸 Что проверить на скриншоте

Если вы видите настройки сервиса, проверьте:
- Есть ли поле "Build Context" или "Context"?
- Где оно находится? (Git Source / Advanced / General / Build)
- Какое там значение? (должно быть `.` или пусто)

## ⚠️ Важно

Build Context - это **НЕ** то же самое, что Base Directory!

- **Build Context** = откуда Docker начинает сборку (должен быть корень репозитория `.`)
- **Base Directory** = базовая директория для относительных путей (может быть `.` или пусто)
- **Dockerfile Location** = путь к Dockerfile относительно Base Directory (должен быть `bot/Dockerfile`)

