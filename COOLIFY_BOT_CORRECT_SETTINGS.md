# ✅ Правильные настройки для Bot сервиса в Coolify

## ❌ Текущая ошибка

```
ERROR: failed to read dockerfile: open v084s8s0ck0s0cs0kkcoc0c0: no such file or directory
```

**Причина:** Поля перепутаны! `Docker Build Stage Target` используется вместо `Dockerfile Location`.

## ✅ Правильные настройки

### В разделе "General" → "Build":

1. **Base Directory:**
   - ❌ Сейчас: `/`
   - ✅ Должно быть: `.` (одна точка, БЕЗ слеша)
   - Или оставьте **пустым** (если Coolify позволяет)

2. **Dockerfile Location:**
   - ❌ Сейчас: `/`
   - ✅ Должно быть: `bot/Dockerfile` (БЕЗ начального `/`)
   - Это путь к файлу Dockerfile относительно Base Directory

3. **Docker Build Stage Target:**
   - ❌ Сейчас: `bot/Dockerfile`
   - ✅ Должно быть: **пусто** (оставьте поле пустым)
   - Это поле используется только для multi-stage builds, чтобы указать конкретный stage

## 📋 Пошаговая инструкция

1. Откройте раздел **"General"** → **"Build"**

2. **Base Directory:**
   - Удалите `/`
   - Введите: `.` (одна точка)
   - Или оставьте пустым

3. **Dockerfile Location:**
   - Удалите `/`
   - Введите: `bot/Dockerfile` (БЕЗ начального `/`)

4. **Docker Build Stage Target:**
   - Очистите поле (оставьте пустым)
   - Это НЕ то же самое, что Dockerfile Location!

5. Нажмите **"Save"**

6. Нажмите **"Deploy"**

## 🔍 Почему backend работает, а bot нет?

Скорее всего, в backend правильно настроены:
- **Base Directory:** `.` или пусто
- **Dockerfile Location:** `backend/Dockerfile`
- **Docker Build Stage Target:** пусто

Примените те же настройки к bot, только измените Dockerfile Location на `bot/Dockerfile`.

## ⚠️ Важно

- **Dockerfile Location** = путь к файлу Dockerfile (например, `bot/Dockerfile`)
- **Docker Build Stage Target** = имя stage в multi-stage build (например, `production`), НЕ путь к файлу!

## ✅ Итоговые настройки

### Bot сервис:
- **Base Directory:** `.` (точка) или пусто
- **Dockerfile Location:** `bot/Dockerfile` (без `/`)
- **Docker Build Stage Target:** пусто

### Backend сервис (для сравнения):
- **Base Directory:** `.` (точка) или пусто
- **Dockerfile Location:** `backend/Dockerfile` (без `/`)
- **Docker Build Stage Target:** пусто

После этих изменений сборка должна пройти успешно!

