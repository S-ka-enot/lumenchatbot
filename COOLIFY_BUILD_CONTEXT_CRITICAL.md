# 🚨 КРИТИЧЕСКИ ВАЖНО: Build Context для Bot сервиса

## ❌ Текущая ошибка

```
ERROR: "/backend/poetry.lock": not found
```

**Причина:** Build Context установлен неправильно. Docker не видит директорию `backend/`.

## ✅ Решение

Build Context в Coolify - это **ОТДЕЛЬНОЕ** поле, не то же самое, что Base Directory!

### Где найти Build Context:

1. Откройте ваш **Bot** сервис в Coolify
2. Перейдите в раздел **"Git Source"** (в левом меню)
3. ИЛИ перейдите в **"Advanced"** (в левом меню)
4. Найдите поле **"Build Context"** или **"Context"** или **"Build Directory"**

### Правильные настройки:

#### В разделе "Git Source" или "Advanced":

- **Build Context:** `.` (одна точка) или **пусто** ✅
- Это означает: корень репозитория (где находятся `backend/`, `bot/`, `frontend/`)

#### В разделе "General" → "Build":

- **Base Directory:** `.` (одна точка, БЕЗ `./`) или пусто
- **Dockerfile Location:** `bot/Dockerfile` (БЕЗ начального `/`)

## 🔍 Как проверить

После настройки Build Context = `.`, Docker должен видеть:
- ✅ `backend/pyproject.toml` - существует
- ✅ `backend/poetry.lock` - существует  
- ✅ `bot/Dockerfile` - существует
- ✅ `bot/app/` - существует

## 📋 Полная конфигурация Bot сервиса

### Git Source / Advanced:
- **Build Context:** `.` (точка) или пусто

### General → Build:
- **Base Directory:** `.` (точка) или пусто
- **Dockerfile Location:** `bot/Dockerfile` (без `/` в начале)

### Network:
- **Ports Exposes:** можно убрать (боту порт не нужен)

## ⚠️ Если не можете найти Build Context

Если в Coolify нет отдельного поля "Build Context":

1. **Base Directory** должен быть: `.` (корень репозитория)
2. **Dockerfile Location** должен быть: `bot/Dockerfile` (относительный путь)

Но обычно Build Context - это отдельное поле в разделе "Git Source" или "Advanced".

## 🔧 Альтернативное решение (если ничего не помогает)

Если Coolify не позволяет установить Build Context правильно, можно изменить Dockerfile:

1. Скопировать `backend/pyproject.toml` и `backend/poetry.lock` в `bot/` перед сборкой
2. Или использовать multi-stage build с другим подходом

Но это не рекомендуется - лучше исправить Build Context!

